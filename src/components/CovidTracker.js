import React from 'react';

export default function CovidTracker() {
    return (
      <div className="about">
        <h1>CovidTracker</h1>
        <p>TO DO: using an API</p>
      </div>
    );
  }